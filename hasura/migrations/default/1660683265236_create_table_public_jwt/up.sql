CREATE TABLE "public"."jwt" ("user_id" uuid NOT NULL, "token" text NOT NULL, PRIMARY KEY ("user_id") , FOREIGN KEY ("user_id") REFERENCES "public"."users"("id") ON UPDATE restrict ON DELETE restrict);
